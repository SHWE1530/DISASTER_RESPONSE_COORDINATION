"""Integration Engineer entrypoint.

The runnable integration service lives in ``backend/app``. This module remains
as the Stage-01 pipeline handoff point until team contracts are finalized.
"""


def main() -> None:
	raise RuntimeError("PENDING TEAM INPUT: upstream team contracts are not finalized")


if __name__ == "__main__":
	main()
