# Disaster Response Coordination - pinned dependencies
#
# These versions are the ones the committed model artifacts were produced with.
# Pinning matters here: the Stage 03 joblib artifacts were originally pickled
# under scikit-learn 1.9.0 and failed at predict time under 1.6.0
# ("LogisticRegression object has no attribute 'multi_class'"). Do not float
# scikit-learn without retraining Stage 01 and Stage 03.
#
#   python -m venv .venv
#   .\.venv\Scripts\Activate.ps1        # Windows
#   source .venv/bin/activate           # Linux / macOS
#   pip install -r requirements.txt

# --- core numerics / data ---
numpy==2.2.1
pandas==2.2.3
scipy==1.17.1

# --- classical ML (Stage 01, Stage 03) ---
scikit-learn==1.6.0
xgboost==3.2.0
lightgbm==4.7.0
joblib==1.4.2

# --- deep learning (Stage 02) ---
torch==2.14.0
torchvision==0.29.0
pillow==12.3.0

# --- transformers (Stage 03 urgency classifier candidate) ---
transformers==5.16.1

# --- visualisation / reporting ---
matplotlib==3.11.1
seaborn==0.13.2

# --- web application ---
flask==3.1.3

# --- testing ---
pytest==8.3.4
